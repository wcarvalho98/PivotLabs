<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ufrpe.br.pivotlabs">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".QuemSomosActivity">
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".login.ui.UserArea">
        </activity>
        <activity android:name=".login.ui.AreaUsuarioActivity">
        </activity>
        <activity
                android:name=".login.ui.FiltragemActivity"
                android:label="@string/title_activity_filtragem"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".login.ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>